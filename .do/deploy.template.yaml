spec:
  name: website
  static_sites:
    - name: website
      github:
        repo: arnu515/website
        branch: master
        deploy_on_push: true
      dockerfile_path: .do/Dockerfile
      output_dir: /app/dist
  
